#include <string>
#include <vector>

using namespace std;

string solution(int a, int b) {
    vector<string> weeks = {"SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"};
    string answer = weeks[0];
    return answer;
}